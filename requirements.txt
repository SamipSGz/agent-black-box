# --- Agent + LLM ---
openai>=1.40
langgraph>=0.2.0
langchain-core>=0.3.0

# --- OpenTelemetry (traces + metrics + logs over OTLP/HTTP) ---
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# --- SRE copilot service ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# --- misc ---
python-dotenv>=1.0.1
pydantic>=2.7.0
